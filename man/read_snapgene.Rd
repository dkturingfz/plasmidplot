% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-snapgene.R
\name{read_snapgene}
\alias{read_snapgene}
\title{Read a plasmid from a SnapGene file}
\usage{
read_snapgene(
  file,
  name = NULL,
  types = NULL,
  skip_types = character(),
  label_from = c("label", "name", "gene", "product", "note"),
  colors = c("style", "file"),
  color_by = c("feature", "type"),
  arrows = TRUE,
  sequence = TRUE
)
}
\arguments{
\item{file}{Path to a \code{.dna} file.}

\item{name}{Plasmid name. Defaults to the file name without its extension.}

\item{types}{Feature types to keep. \code{NULL} keeps everything not in
\code{skip_types}.}

\item{skip_types}{Feature types to drop.}

\item{label_from}{Qualifier names to try, in order, when labeling a
feature. \code{"name"} is the feature's own name attribute.}

\item{colors}{\code{"style"} takes colors from the plot style's palette;
\code{"file"} keeps the colors stored in the SnapGene file.}

\item{color_by}{\code{"feature"} gives every feature its own palette color;
\code{"type"} gives all features of one type the same color.}

\item{arrows}{Draw arrowheads for stranded features (default \code{TRUE}).
SnapGene's bidirectional features become double-headed arrows.}

\item{sequence}{Keep the sequence on the result (default \code{TRUE}).
\code{\link[=pp_find_sites]{pp_find_sites()}} needs it.}
}
\value{
A \code{plasmid} object.
}
\description{
Parses a SnapGene \code{.dna} file: sequence length and topology from the
sequence segment, and features (name, type, range, strand, color) from the
embedded feature table.
}
\details{
SnapGene records carry no plasmid name, so the file's base name is used
unless \code{name} is given.
}
\note{
Requires the \code{xml2} package, which is only needed for this reader.
}
\examples{
dna <- system.file("extdata", "pDemo.dna", package = "plasmidplot")
p <- read_snapgene(dna)
p
plot(p, style = "snapgene")

# Keep the colors the file itself stores, rather than the style's palette.
plot(read_snapgene(dna, colors = "file"))
}
\seealso{
\code{\link[=read_genbank]{read_genbank()}}, \code{\link[=read_plasmid]{read_plasmid()}}
}
