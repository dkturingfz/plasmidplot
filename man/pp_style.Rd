% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{pp_style}
\alias{pp_style}
\title{Create or customize a plasmid map style}
\usage{
pp_style(preset, ...)
}
\arguments{
\item{preset}{Base preset to start from; see the table above. Call
\code{pp_style()} with no arguments for the names.}

\item{...}{Named overrides. The shape parameters are \code{layout}, \code{anchor},
\code{backbone}, \code{radius}, \code{track}, \code{arc} and \code{gap}. The rest are:
\code{bg} (\code{NA} in every preset — styles do not paint a background; see
\code{\link[=pp_canvas]{pp_canvas()}}), \code{canvas}, \code{track_fill}, \code{track_col}, \code{backbone_col},
\code{backbone_lwd}, \code{palette} (a \code{\link[=pp_palette]{pp_palette()}} name, a vector of colors, or
a function of \code{n} returning \code{n} colors — the shape \code{ggsci},
\code{RColorBrewer}, \code{scales} and \code{viridis} all expose, so those drop straight
in; check any of them with \code{\link[=pp_check_palette]{pp_check_palette()}}), \code{arc_border} (\code{NA},
\code{"auto"} for a darker step of the fill, or a color), \code{arc_lwd},
\code{arrow_deg}, \code{arrow_flare}, \code{label_r}, \code{label_fontsize},
\code{label_fontface}, \code{label_col}, \code{leader_col}, \code{leader_lwd}, \code{site_col}
(\code{NA} follows \code{label_col}), \code{site_lwd}, \code{site_len}, \code{site_fontsize},
\code{tick_every} (\code{NA} = automatic), \code{tick_len}, \code{tick_col}, \code{tick_fontsize},
\code{tick_labels}, \code{show_title}, \code{title_col}, \code{title_fontsize},
\code{subtitle_col}, \code{subtitle_fontsize}.}
}
\value{
An object of class \code{pp_style}, or a character vector of preset
names when called with no arguments.
}
\description{
A style is built from a handful of \strong{shape} parameters plus secondary ink
and type settings. Set the shape parameters directly; the presets are only
named combinations of them, and everything a preset does can be written out
by hand.
}
\section{Shape}{

These decide the construction of the map. They are the arguments worth
reaching for first:

\describe{
\item{\code{layout}}{\code{"auto"} draws a circular map for a circular plasmid and
a linear one for a linear plasmid, following the object's \code{topology}.
Force it with \code{"circular"} or \code{"linear"}.}
\item{\code{anchor}}{Where features sit relative to the backbone: \code{"center"}
on it, \code{"outside"} clear of it (outward on a circle, above the line on
a linear map), or \code{"inside"}.}
\item{\code{backbone}}{\code{"ring"} draws the backbone as a filled band, \code{"line"}
as a single stroke, \code{"none"} omits it.}
\item{\code{radius}}{Circular only: the backbone's radius, in npc units of a
square viewport, so the usable maximum is 0.5.}
\item{\code{track}}{Backbone thickness, used when \code{backbone = "ring"}.}
\item{\code{arc}}{Feature thickness.}
\item{\code{gap}}{Clearance between backbone and features when \code{anchor} is
\code{"outside"} or \code{"inside"}.}
}
}

\section{Presets}{

Every preset is exactly the shape below, plus colors. \code{dark} is \code{angular}
restepped for a dark ground, which is why their shapes match.\tabular{lllllll}{
   preset \tab layout \tab anchor \tab backbone \tab radius \tab track \tab arc \cr
   \code{angular} \tab auto \tab center \tab ring \tab 0.30 \tab 0.045 \tab 0.058 \cr
   \code{classic} \tab auto \tab outside \tab line \tab 0.26 \tab 0.045 \tab 0.050 \cr
   \code{dark} \tab auto \tab center \tab ring \tab 0.30 \tab 0.045 \tab 0.058 \cr
   \code{snapgene} \tab auto \tab center \tab ring \tab 0.30 \tab 0.012 \tab 0.042 \cr
   \code{soft} \tab auto \tab center \tab ring \tab 0.28 \tab 0.090 \tab 0.090 \cr
   \code{neon} \tab auto \tab outside \tab ring \tab 0.25 \tab 0.030 \tab 0.050 \cr
   \code{minimal} \tab auto \tab center \tab line \tab 0.30 \tab 0.045 \tab 0.022 \cr
   \code{blueprint} \tab auto \tab inside \tab line \tab 0.33 \tab 0.045 \tab 0.050 \cr
}


Their default palettes are \code{default}, except \code{soft} (\code{muted}), \code{dark} and
\code{blueprint} (\code{default_dark}) and \code{neon} (\code{neon_dark}). A palette is an
independent choice — see \code{\link[=pp_palette]{pp_palette()}}.
}

\examples{
pp_style()
p <- pp_demo_plasmid()

# Compose a style from the shape parameters, no preset involved.
plot(p, style = pp_style(anchor = "outside", backbone = "line",
                         radius = 0.26, arc = 0.05))

# Or start from a preset and change one thing.
plot(p, style = pp_style("angular", arc = 0.09))
plot(p, style = pp_style("minimal", layout = "linear"))

# Any palette vector or palette function works, e.g.
# pp_style("angular", palette = ggsci::pal_npg("nrc"))
plot(p, style = pp_style("angular", palette = grDevices::hcl.colors(8, "Set2")))
}
