% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{pp_find_sites}
\alias{pp_find_sites}
\title{Find restriction sites in the plasmid sequence}
\usage{
pp_find_sites(
  p,
  enzymes = NULL,
  unique_only = TRUE,
  max_sites = 3L,
  show_position = TRUE
)
}
\arguments{
\item{p}{A \code{plasmid} object carrying a sequence (the readers keep one;
\code{\link[=read_fasta]{read_fasta()}} gives you a bare backbone with just the sequence).}

\item{enzymes}{Enzyme names from \code{\link[=pp_enzymes]{pp_enzymes()}}, or a named character
vector of recognition sequences (IUPAC codes allowed), e.g.
\code{c(MyEnz = "GGWCC")}. \code{NULL} searches every enzyme in \code{\link[=pp_enzymes]{pp_enzymes()}}.}

\item{unique_only}{Keep only enzymes that cut exactly once (default
\code{TRUE}). This is the classic plasmid-map view: an enzyme cutting a dozen
times adds no information and buries the map in labels.}

\item{max_sites}{Drop enzymes cutting more than this many times. Ignored
when \code{unique_only} is \code{TRUE}.}

\item{show_position}{Append the cut position to each label, e.g.
\code{"EcoRI (396)"}.}
}
\value{
The updated \code{plasmid} object.
}
\description{
Searches the sequence kept by the readers and adds a \code{\link[=pp_site]{pp_site()}} for each
cut position. On a circular plasmid the search wraps the origin, so a site
straddling position 1 is not missed.
}
\examples{
p <- read_genbank(system.file("extdata", "pDemo.gb", package = "plasmidplot"))
p <- pp_find_sites(p, c("EcoRI", "BamHI", "HindIII", "NotI"))
p
plot(p)

# Every unique cutter the built-in table knows about:
plot(pp_find_sites(read_genbank(
  system.file("extdata", "pDemo.gb", package = "plasmidplot"))))
}
\seealso{
\code{\link[=pp_site]{pp_site()}}, \code{\link[=pp_enzymes]{pp_enzymes()}}
}
