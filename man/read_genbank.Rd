% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-genbank.R
\name{read_genbank}
\alias{read_genbank}
\title{Read a plasmid from a GenBank file}
\usage{
read_genbank(
  file,
  name = NULL,
  types = NULL,
  skip_types = "source",
  label_from = c("label", "gene", "product", "standard_name", "note", "locus_tag"),
  colors = c("style", "file"),
  color_by = c("feature", "type"),
  arrows = TRUE,
  sequence = TRUE
)
}
\arguments{
\item{file}{Path to a \code{.gb}, \code{.gbk}, or \code{.genbank} file.}

\item{name}{Plasmid name. Defaults to the \code{LOCUS} identifier.}

\item{types}{Feature types to keep (e.g. \code{c("CDS", "rep_origin")}).
\code{NULL} keeps everything not in \code{skip_types}.}

\item{skip_types}{Feature types to drop. \code{"source"} spans the whole
plasmid and is dropped by default.}

\item{label_from}{Qualifier names to try, in order, when labeling a
feature. Falls back to the feature type.}

\item{colors}{\code{"style"} takes colors from the plot style's palette.
\code{"file"} honors \verb{/ApEinfo_fwdcolor} qualifiers when the record has them
(SnapGene and ApE write these), falling back to the style palette for
features without one.}

\item{color_by}{\code{"feature"} gives every feature its own palette color, so
neighbors stay distinguishable. \code{"type"} gives all features of one type
the same color, which reads as a legend of feature classes instead.}

\item{arrows}{Draw arrowheads for stranded features (default \code{TRUE}).}

\item{sequence}{Keep the \code{ORIGIN} sequence on the result (default \code{TRUE}).
\code{\link[=pp_find_sites]{pp_find_sites()}} needs it.}
}
\value{
A \code{plasmid} object.
}
\description{
Parses the \code{LOCUS} line for the name and length and the \code{FEATURES} block
for markers. Handles \code{complement(...)}, \code{join(...)}, and the \code{<}/\code{>}
partial-boundary markers. A \code{join()} whose first segment starts after its
last segment ends is kept as an origin-crossing feature.
}
\examples{
gb <- system.file("extdata", "pBR322.gb", package = "plasmidplot")
p <- read_genbank(gb)
p
plot(p)
}
\seealso{
\code{\link[=read_snapgene]{read_snapgene()}}, \code{\link[=read_plasmid]{read_plasmid()}}, \code{\link[=pp_find_sites]{pp_find_sites()}}
}
