% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.plasmid}
\alias{plot.plasmid}
\title{Plot a plasmid map}
\usage{
\method{plot}{plasmid}(x, style = "angular", bg = NULL, newpage = TRUE, ...)
}
\arguments{
\item{x}{A \code{plasmid} object.}

\item{style}{A preset name or a \code{\link[=pp_style]{pp_style()}} object. Call \code{pp_style()} for
the available presets: \code{"angular"}, \code{"classic"}, \code{"dark"}, \code{"snapgene"},
\code{"soft"}, \code{"neon"}, \code{"minimal"}, \code{"blueprint"}.}

\item{bg}{Background to paint behind the map. \code{NULL} (the default) paints
nothing, so the device or surrounding viewport shows through -- styles
never impose a background of their own. Pass a color, or
\code{pp_canvas(style)} for the ground the style was designed against, which
the dark presets need to be legible.}

\item{newpage}{Start a new grid page first (default \code{TRUE}). Set to
\code{FALSE} to draw into an existing viewport, e.g. for panel layouts.}

\item{...}{Ignored.}
}
\value{
Invisibly, \code{x}.
}
\description{
Renders the plasmid as a circular or linear map: a backbone with a
base-pair scale, colored feature arcs (with optional arrowheads), callout
labels with leader lines, restriction sites, and the plasmid name and size.
}
\details{
The layout follows the plasmid's \code{topology} unless the style says
otherwise; see the \code{layout} shape parameter in \code{\link[=pp_style]{pp_style()}}.
}
\examples{
p <- plasmid("pBR322", 4361) |>
  pp_marker(86, 1276, label = "TcR", arrow = "end") |>
  pp_marker(2535, 3122, label = "ori") |>
  pp_marker(3293, 4153, label = "AmpR", arrow = "start")
plot(p, style = "angular")
plot(p, style = "dark", bg = pp_canvas("dark"))
plot(p, style = pp_style("angular", layout = "linear"))
}
