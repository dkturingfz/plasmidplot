% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plasmid.R
\name{pp_marker}
\alias{pp_marker}
\title{Add a feature marker to a plasmid}
\usage{
pp_marker(
  p,
  start,
  end,
  label = NULL,
  color = NULL,
  group = NULL,
  arrow = c("none", "end", "start", "both"),
  offset = 0,
  width = NULL
)
}
\arguments{
\item{p}{A \code{plasmid} object created by \code{\link[=plasmid]{plasmid()}}.}

\item{start, end}{Feature boundaries in base pairs (1-based, inclusive).}

\item{label}{Optional text label, drawn outside the map with a leader line.}

\item{color}{Arc color. Defaults to the next color of the style palette.}

\item{group}{Optional grouping key. Markers sharing a group take the same
palette color, so e.g. every CDS can be drawn in one color without
naming the color here. Ignored when \code{color} is given.}

\item{arrow}{Arrowhead placement: \code{"none"}, \code{"end"} (points clockwise, for
a forward-strand feature), \code{"start"} (counter-clockwise, reverse strand),
or \code{"both"} (bidirectional).}

\item{offset}{Radial offset from the backbone in npc units. Positive moves
the marker outward; useful to separate overlapping features.}

\item{width}{Arc thickness in npc units. Defaults to the style's
\code{arc}.}
}
\value{
The updated \code{plasmid} object.
}
\description{
Markers are drawn as colored arcs on the plasmid backbone. A marker whose
\code{end} is smaller than its \code{start} is taken to wrap across the origin
(position 1). Calls are pipe-friendly: each returns the updated object.
}
\examples{
p <- plasmid("pUC19", 2686) |>
  pp_marker(146, 469, label = "lacZ-alpha", arrow = "end") |>
  pp_marker(1629, 2486, label = "AmpR", arrow = "start")
plot(p)
}
