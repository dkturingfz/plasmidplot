% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites.R
\name{pp_features}
\alias{pp_features}
\title{Add many features at once from a data frame}
\usage{
pp_features(p, df)
}
\arguments{
\item{p}{A \code{plasmid} object.}

\item{df}{A data frame with columns \code{start} and \code{end}, and optionally
\code{label}, \code{color}, \code{group}, \code{arrow}, \code{offset}, \code{width}.}
}
\value{
The updated \code{plasmid} object.
}
\description{
A vectorized \code{\link[=pp_marker]{pp_marker()}}: one row per feature. Only \code{start} and \code{end}
are required; any other column named after a \code{\link[=pp_marker]{pp_marker()}} argument is
used, and unknown columns are ignored.
}
\examples{
feats <- data.frame(
  start = c(86, 2535, 3293),
  end   = c(1276, 3122, 4153),
  label = c("TcR", "ori", "AmpR"),
  arrow = c("end", "none", "start")
)
plot(pp_features(plasmid("pBR322", 4361), feats))
}
