% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-formats.R
\name{read_fasta}
\alias{read_fasta}
\title{Read a plasmid from a FASTA file or a bare sequence}
\usage{
read_fasta(file, name = NULL, circular = TRUE, ...)
}
\arguments{
\item{file}{Path to a FASTA file, or a file holding only sequence letters.}

\item{name}{Plasmid name. Defaults to the FASTA header (up to its first
space), or the file name when there is no header.}

\item{circular}{Whether the sequence is circular (default \code{TRUE}). FASTA
records no topology.}

\item{...}{Ignored, so \code{\link[=read_plasmid]{read_plasmid()}} can pass reader arguments through.}
}
\value{
A \code{plasmid} object.
}
\description{
FASTA carries no feature table, so the result is a bare backbone: name,
length and sequence, with no markers. Add features with \code{\link[=pp_marker]{pp_marker()}}, or
restriction sites with \code{\link[=pp_find_sites]{pp_find_sites()}}, which needs exactly this.
}
\examples{
fa <- tempfile(fileext = ".fa")
writeLines(c(">pMini test plasmid", strrep("ATGC", 500)), fa)
p <- read_fasta(fa)
p
unlink(fa)
}
