# cran-comments

## Submission

This is a new release.

## Test environments

- Windows 11, R 4.6.0 (local): `R CMD check --as-cran`
- <!-- fill in before submitting -->
  - win-builder, R devel and R release
  - macOS builder
  - R-hub: linux, macos, windows

## R CMD check results

0 errors | 0 warnings | 0 notes

## Notes for the reviewer

- The package has no dependencies outside base R and the recommended
  packages; `xml2` is only needed to read 'SnapGene' files and is in Suggests,
  guarded by `requireNamespace()`.
- Two examples use `\dontrun{}`: they illustrate passing palettes from
  `ggsci` and `RColorBrewer`, which are not declared dependencies and so
  cannot run on the check machine. Every other example runs.
- `inst/extdata/` holds four small fixture files (GenBank, EMBL, FASTA and a
  minimal binary 'SnapGene' record) so the readers' examples and tests run
  against real files. They are generated by `dev/make-fixtures.R` and total
  under 15 kB.
